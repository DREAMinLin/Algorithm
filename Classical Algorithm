1.Bubble Sort：越大的元素会经由（相邻）交换慢慢“浮”到数列的顶端（底部）。第i次遍历会“浮”到第n+1-i个位置。
2.Selection Sort：对于未排序数据，通过指针查找最大（小）的数，存放（交换）到相应顺序位置。第i次遍历会将数存放到第n+1-i个位置。
3.Insertion Sort：对于未排序数据，在已排序序列中从后向前扫描，找到相应位置并插入。
4.Quick Sort：每次遍历将要排序的数据分割成独立的两部分，以一个数据为基准，一部分的所有数据都比该数小，另外一部分的所有数据都比该数大，然后再按此方法对这两部分数据再分别进行快速排序（递归）。
5.
